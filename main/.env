# ---------------------------------------------------
# Remplir les valeurs pour les 3 variables ci-dessous
# ---------------------------------------------------

# Mot de passe de l'utilisateur 'elastic' 
# A REMPLIR OBLIGATOIREMENT (6 caractères ou plus, sans espaces, ', $, #, @ etc.)
ELASTIC_PASSWORD=

# Mot de passe de l'utilisateur 'kibana_system'
# A REMPLIR OBLIGATOIREMENT (6 caractères ou plus, sans espaces, ', $, #, @ etc.)
KIBANA_PASSWORD=

# Nom du cluster
# A REMPLIR OBLIGATOIREMENT AVEC UN NOM UNIQUE
CLUSTER_NAME=


# ---------------------------------------------------
# Ne rien modifier sous cette ligne
# ---------------------------------------------------

# Version des produits Elastic
STACK_VERSION=8.9.2

# Licence basic, permettant de démarrer une période d'essai de 30 jours
LICENSE=basic

# Port exposé au niveau de l'hôte Docker pour l'API Elasticsearch (ici le port par défaut)
ES_PORT=9200

# Port exposé au niveau de l'hôte Docker pour Kibana (ici HTTPS standard)
KIBANA_PORT=443

# Quantité de mémoire (en octets) allouée à chaque noeud (Ici 8Go)
MEM_LIMIT=8589934592

# Espace de nom du projet compose
COMPOSE_PROJECT_NAME=elk

# URL internet publique de kibana
PUBLIC_URL=https://${NAME}.francecentral.cloudapp.azure.com
